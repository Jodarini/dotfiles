# Add your additional Hyprland configurations here
#
# This is an additional key binding
# bind = $mainMod CTRL, up, workspace, empty
#
# Example for xwayland
# xwayland {
#   force_zero_scaling = true
# }

# qt5ct environment variable
# env = QT_QPA_PLATFORMTHEME,qt5ct

# SDL version
env = SDL_VIDEODRIVER,wayland
# env = SDL_VIDEODRIVER,x11

# No Hardware Cursor
# cursor {
#     no_hardware_cursors = false
# }

# Blur for waybar
#layerrule = blur, waybar

input {
    repeat_delay = 250   # Delay in milliseconds
    repeat_rate = 30    # Rate in Hz
    # accel_profile=flat
    # follow_mouse=1
    # force_no_accel=1
    # sensitivity=1
}

# AUTOSTART
exec-once = walker --gapplication-service
exec-once = [workspace 1 silent] kitty
exec-once = [workspace 4 silent] zen-browser
exec-once = [workspace 5 silent] /opt/brave-bin/brave --profile-directory=Default --app-id=hnpfjngllnobngcgfapefoaidbinmjnm
